name: digit_assignment
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.0 <=4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  digit_components: ^0.0.2+1
  reactive_forms: ^14.1.0
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
